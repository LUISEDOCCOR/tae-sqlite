# 📋 Sistema de Empleados

Este proyecto es un sistema sencillo para registrar, ver, editar y eliminar empleados usando  
Python, SQLite y Streamlit.

La información se guarda en una base de datos local (`basededatos.db`).

---

## ⚙️ Requisitos

```bash
pip install -r requirements.txt
streamlit run main.py
```
